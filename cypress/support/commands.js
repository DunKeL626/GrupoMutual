import {HeadUp} from './commands/head-up';
import {HeadDown} from './commands/head-down';

Cypress.Commands.add('HeadFooterFlyBottom',(zise)=>{
    // HeadUp(zise);
    // cy.reload();
    // HeadDown(zise);
    
});